# Requirements for tts_server.py's isolated venv (venv-tts/), kept
# separate from requirements.txt on purpose — chatterbox-tts pins
# transformers==5.2.0/torch==2.6.0, which conflicts with requirements.txt's
# transformers/torch pins used by api_server.py's LLM/vision/embedding
# models (confirmed empirically 2026-08-08: installing chatterbox-tts into
# that shared venv broke sentence-transformers, the transformers LLM
# pipeline, and LlavaNext vision imports).
#
# Setup:
#   python3 -m venv venv-tts && . venv-tts/bin/activate
#   pip install -r requirements-tts.txt

chatterbox-tts
fastapi
uvicorn
